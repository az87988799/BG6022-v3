# R0 evidence summary

阶段：R0
方案版本：R0-1.0
reference_base_commit：`72b0c3cffe514a84d46dd681f8e3fb4f7aa9341f`
actual_base_commit：`72b0c3cffe514a84d46dd681f8e3fb4f7aa9341f`
tested_commit：`b472c58c4ba732d6086702bc15add830351ed8a9`
documentation_commit：pending
production_code_changes：none

## 基线

- Python 3.11.4 / Windows，项目环境在仓库外 `E:\BG6022-r0-venv`。
- `uv 0.12.17` 在一次性工具环境中安装；`uv lock --check` 和 frozen dev sync 通过。
- 原始基线：391 collected；离线 375 passed、16 deselected；Windows 2 passed、389 deselected；ruff、format、compileall、diff-check、source/wheel build 通过。
- 候选回归：396 collected；离线 377 passed、3 strict xfailed、16 deselected；Windows 2 passed、394 deselected；退出码均为 0。

## 新增保护和缺口证据

- 新增真实身份补充链：真实 `Request → Plan` 校验、替身 PubChem、RDKit 几何、`Agent.advance`，确认前 ORCA 启动数为 0；通过。
- 新增取消/确认交错探针；在确认持久化与推进之间取消，工具启动数为 0；通过。
- `R1-GAP-TOOL-UNEXPECTED-EXCEPTION`：工具 `RuntimeError` 越过当前 `Agent.advance`，严格 xfail。
- `R1-GAP-RESULT-PERSISTENCE`：隔离 `save_result` 的 `OSError` 越过 Run 生命周期，严格 xfail。
- `R1-GAP-CONFIRM-RACE`：两个确认同时消费同一等待授权并启动同一测试 Tool 两次，严格 xfail。
- 三项缺口以 `--runxfail` 精确 nodeid 复跑，原始失败日志在 `E:\BG6022-r0-evidence-20260922`，未将失败伪装为通过。

## Wheel / live / 隔离

- 最终候选 wheel 在仓库外独立 Python 3.11 环境中安装；`python -I` 导入位置为该环境的 site-packages，四份 prompt hash 与源码一致，`python -I -m bg6022 --help` 通过。
- 真实 LLM、PubChem、ORCA：`not_run`；live marker 排除，测试使用 `--disable-socket`。
- 原工作区未跟踪的本地诊断 Run/Artifact 文件未触碰；旧数据根 `E:\BG6022-v3-data` 未用作测试 data root。
- 当前默认资源仍为 4 cores、1024 MB、`%maxcore 192`、并发 1。

工程准备结论：`ready_for_r1_repair`。
用户接受：`awaiting_user_acceptance`。
R1 第一批：统一 Tool 普通异常闭合、Result/Run 持久化失败闭合、确认授权单次消费边界。
